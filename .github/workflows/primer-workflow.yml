name: Mi segundo Workflow

on:
  push:

jobs:
  explorando-maquina:

    runs-on: ubuntu-latest

    steps:

      - name: Mostrar carpeta actual
        run: pwd

      - name: Mostrar contenido de la carpeta
        run: ls -la

      - name: Intentar leer README
        run: cat README.md