name: Mi cuarto Workflow

on:
  push:

jobs:
  experimento-error:

    runs-on: ubuntu-latest

    steps:

      - name: Paso 1
        run: echo "Iniciando workflow"

      - name: Paso 2
        run: comando_que_no_existe

      - name: Paso 3
        run: echo "¿Llegué hasta aquí?"